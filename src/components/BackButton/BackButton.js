import React from 'react';
import {NavLink} from "react-router-dom";

const BackButton = () => {


    return (
        <NavLink
            to="/login"
        >Atgal</NavLink>
    )
}

export default BackButton;
